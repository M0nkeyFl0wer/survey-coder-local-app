# Survey Coder - Environment Configuration Example
# Copy this file to .env and fill in your actual values

# OpenAI API Key (required for AI features)
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Custom database path
# CODER_APP_DB_PATH=/path/to/custom/database.db

# Optional: Custom data directory
# CODER_APP_DATA_DIR=/path/to/custom/data/dir

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
# CODER_LOG_LEVEL=INFO

# Optional: SSH configuration for remote agents (future feature)
# SSH_PRIVATE_KEY_PATH=/path/to/ssh/key
# REMOTE_AGENTS_CONFIG_PATH=/path/to/agents/config.json